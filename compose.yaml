version: '3'
services:
  client:
    build: 
      context: .
      dockerfile: ./Dockerfile 
    image: manulangat/blog_by_langat:blog-fe
    restart: on-failure
    # volumes:
    #   - .:/app
      # - /app/node_modules 
    networks:
      - blog-cms-fe

networks:
  blog-cms-fe:
# volumes:
#   /app/node_modules:
